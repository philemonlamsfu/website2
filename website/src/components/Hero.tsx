const Hero = () => {
  return (
    <section className="hero">
      <div className="hero-inner">
        <h1>Making data <em>trustworthy.</em></h1>
      </div>
    </section>
  );
};

export default Hero;